<%= form_with(model: room, local: true) do |form| %>
  <% if room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :kind %>
    <%= form.text_field :kind, id: :room_type %>
  </div>

  <div class="field">
    <%= form.label :size %>
    <%= form.text_field :size, id: :room_size %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :room_price %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note, id: :room_note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
